apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tasmota-watcher.fullname" . }}-test-connection"
  labels:
    {{- include "tasmota-watcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tasmota-watcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
